import React from 'react';
import ReactDOM from 'react-dom';
import Booklist from './booklist';

/**
 * JSX Rules 
 * return a single element
 * div section article
 * use camelCase for html atribute
 * className 
 * close every element 
 * formatting
 * Split application to simple chunks
 */

ReactDOM.render(
  <Booklist></Booklist>,
  document.getElementById('root')
);